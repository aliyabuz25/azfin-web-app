FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY server/ ./server/
# Create data files if they don't exist
RUN echo "{}" > data.json && echo "[]" > requests.json && mkdir -p uploads
EXPOSE 5001
ENV NODE_ENV=production
CMD ["node", "server/index.js"]
